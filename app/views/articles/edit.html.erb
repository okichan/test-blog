<h1>Edit article</h1>

<% if @article.errors.any? %>
	<div class="error">
		<p><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</p>
		<% @article.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		<% end %>
	</div>
<% end %>


	<%= form_for :article, url: article_path, method: :patch do |f| %>
		<%= f.text_field :title, placeholder: 'Title' %><br>
		<br>
		<%= f.text_area :text, placeholder: 'Write something' %><br>
		<br>
		<%= f.submit %>
	<% end %>

<p><%= link_to 'Back', articles_path %></p>